/* eslint-disable @typescript-eslint/no-namespace */
export interface okrCompletedAndUncompleted {
    uncompleted: number,
    completed: number
}

export interface degreeOfCompletionAndScore {
    completion_sum: number,
    score_sum: number,
    total: number
}